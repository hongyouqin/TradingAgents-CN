## 重新安装tradingagents-backend镜像
### 1. 停止并删除旧容器
docker stop tradingagents-backend
docker rm tradingagents-backend

### 2. （可选）删除旧镜像，确保使用最新代码构建
docker rmi hsliup/tradingagents-backend:latest

### 3. 清理构建缓存（释放空间，避免使用旧缓存）
docker builder prune -f

### 构建后端镜像
sudo docker-compose -f docker-compose.hub.nginx.yml up -d --build backend 

### 导入初始化配置
docker exec -it tradingagents-backend python scripts/import_config_and_create_user.py /app/install/database_init_export_config.json --skip-user
只导入系统配置  
docker exec -it tradingagents-backend python scripts/import_config_and_create_user.py   install/database_init_export_config.json  --collections system_configs


### 下载容器里的日志到本地
docker cp tradingagents-backend:/app/logs/tradingagents.log ./tradingagents.log


### 微信部署验证
1. 根据微信提示，创建文件  
2. 把文件复制到web根目录（web容器）  
docker cp d946efc5f657e19e0e95d5cbcae82d34.txt tradingagents-web:/usr/share/nginx/html/
3. 验证文件存在
docker exec tradingagents-web ls -la /usr/share/nginx/html/d946efc5f657e19e0e95d5cbcae82d34.txt
4. 测试外部访问
curl https://nbstockai.com/d946efc5f657e19e0e95d5cbcae82d34.txt

### 确保nginx容器在失败时自动重启
docker update --restart=unless-stopped tradingagents-nginx


### 微信公众平台测试号获取地址
mp.weixin.qq.com/debug/cgi-bin/sandboxinfo
### 测试通过code获取apendid
https://open.weixin.qq.com/connect/oauth2/authorize?appid=wx4b2ebb414a1f0891&redirect_uri=https://rehearsable-lineable-esperanza.ngrok-free.dev/api/payment/wechat/callback&response_type=code&scope=snsapi_base&state=#wechat_redirect



### windows上安装ta-lib
#### 设置环境
$env:TA_LIB_PATH = "D:\ta-lib"
$env:PATH = "$env:PATH;D:\ta-lib\bin"
#### 安装ta-lib
pip install TA-Lib


### 查看百度统计
https://tongji.baidu.com/main/homepage/self/homepage/index